#!/usr/bin/env bash


GCC=$(/opt/devtools/bin/gcc --version | head -n 1 | awk '{ print $3 }')
BINUTILS=$(/opt/devtools/bin/ld --version | head -n 1 | awk '{ print $5 }')

tar cf - /opt/devtools | bzip2 --best - > gcc-$GCC-binutils-$BINUTILS-$(uname -m).tar.bz2 

